const path = require('path')

module.exports = {
    config: path.resolve(__dirname, 'core', 'config', 'dbconfig.js'),
    'migrations-path': path.resolve(__dirname, 'core', 'database', 'migrations'),
}